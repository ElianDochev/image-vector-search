# App
# Where the app/ingestion reads images from inside the container.
# - Use /app/static for images in ./static or any subdir in repo where you put your images(start with /app/*).

APP_DATA_DIR=/app/static

# LanceDB (file-based vector DB)
LANCEDB_DIR=/app/lancedb

# Streamlit
STREAMLIT_SERVER_PORT=8501
